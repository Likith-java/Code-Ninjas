# Server
PORT=4000
NODE_ENV=development

# Database (SQLite file location)
DB_PATH=./data/dayflow.db

# Auth
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Set to true when serving over HTTPS so the session cookie is marked Secure
COOKIE_SECURE=false
